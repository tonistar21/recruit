import { useQuery } from '@tanstack/react-query';
import { CalendarClock, CheckCircle2, FileSearch, UserPlus, Users } from 'lucide-react';
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { PageHeader, Loading } from '../components/States';
import { api } from '../services/api';

type Dashboard={total:number;new_week:number;upcoming_interviews:number;by_stage:{name:string;count:number}[]};
export function DashboardPage(){const {data,isLoading}=useQuery({queryKey:['dashboard'],queryFn:()=>api<Dashboard>('/analytics/dashboard')});if(isLoading)return <Loading/>;const cards=[[data?.total,'Усього кандидатів',Users],[data?.new_week,'Нові за тиждень',UserPlus],[data?.by_stage.find(x=>x.name==='Перевірка документів')?.count??0,'Перевірка документів',FileSearch],[data?.upcoming_interviews,'Найближчі співбесіди',CalendarClock],[data?.by_stage.find(x=>x.name==='Прийнято')?.count??0,'Прийнято',CheckCircle2]] as const;return <><PageHeader title="Системний огляд" subtitle="Актуальні показники рекрутингового процесу з бази даних"/><div className="stats">{cards.map(([value,label,Icon])=><article className="stat" key={label}><span><small>{label}</small><b>{value}</b></span><Icon/></article>)}</div><div className="grid-2"><section className="panel"><div className="panel-title"><h2>Розподіл за етапами</h2><span>Усі активні записи</span></div><div className="chart"><ResponsiveContainer><BarChart data={data?.by_stage}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="name" hide/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="count" fill="var(--accent)" radius={[6,6,0,0]}/></BarChart></ResponsiveContainer></div></section><section className="panel"><div className="panel-title"><h2>Потребують уваги</h2><span>Автоматична вибірка</span></div><div className="attention"><FileSearch/><b>Перевірте документи кандидатів</b><p>Записи на етапі перевірки очікують рішення відповідального рекрутера.</p></div></section></div></>}
